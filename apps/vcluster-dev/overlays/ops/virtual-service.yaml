---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: vcluster
  namespace: vcluster-dev
  annotations:
    link.argocd.argoproj.io/external-link: https://dev.k.cluster.sciops.net
spec:
  hosts:
    - "dev.k.cluster.sciops.net"
  gateways:
    - istio-system/istio-ingressgateway
  tls:
    - match:
        - sniHosts:
            - "dev.k.cluster.sciops.net"
      route:
        - destination:
            host: vcluster-dev.vcluster-dev.svc.cluster.local
            port:
              number: 443
